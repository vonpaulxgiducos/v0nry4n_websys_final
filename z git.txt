GIT + GITHUB

CLONING
git clone (link)

Git cheat sheet sa fb, Git fundamentals (init, clone, branch, merge, pull requests). + GitLab / Bitbucket. Search FULL TUTORIAL ANI

PUSH TO GITHUB REPOSITORY
Himo saka repository sa github tapos inig human nimo create adto ka sa terminal nimo sa vstudio

Check Credential Manager anay then

git init
git remote -v
git remote set-url origin (link)
Git remote add origin
git config --global user.email "von.giducos@urios.edu.ph”
git config --global user.name “vonpaulxgiducos" 
or
git config --local user.name
git config --local user.email

PUSH TO NEW MADE GITHUB REPOSITORY
git init
git add . 	
git commit -m “changes sa code”
git remote add origin (http link sa imo file repository)
git branch -m main
git push origin main (git push -u origin main)

///////// WHEN UPDATING THE CODE TO REPO
git status
git init (if transfer/newproject) 
git add .
git commit -m "Removed TailwindCSS and updated webpack.mix.js setup"
git push -u origin main


